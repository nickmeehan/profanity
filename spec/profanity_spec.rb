require "spec_helper"

describe Profanity do
  it "has a version number" do
    expect(Profanity::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
